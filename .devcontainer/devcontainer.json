{
  "name": "Custom Ubuntu Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "export COREPACK_ENABLE_DOWNLOAD_PROMPT=0 &&sudo  corepack enable && yarn set version stable && yarn install"
}
